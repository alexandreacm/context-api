import {
    StyleSheet,
} from 'react-native';
  
export const styles = StyleSheet.create({
    input: {
      width: '85%',
      height: 50,
      padding: 8,
      fontSize: 16,
      color: '#000',
    }
  });
  